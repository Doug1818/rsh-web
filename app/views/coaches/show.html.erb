<div class="container">
  <div class="row">
    <div class="col-md-5 col-sm-4">
      <h2>Your Clients</h2>
    </div>
    <div class="col-md-7 col-sm-8">
      <div class="col-md-7 col-sm-6">
        <ul class="list-inline view">
          <li>View:</li>
          <li><%= link_to 'All', coach_path %></li>
          <li><%= link_to 'Alerts', coach_path(filter: 'alerts') %></li>
          <li><%= link_to 'Active', coach_path(filter: 'active') %></li>
        </ul>
      </div>
      <div class="col-md-5 col-sm-6">
        <%= link_to 'Add a New Client', new_program_path, class: 'btn btn-primary add-patient' %>
      </div>
    </div>
  </div>
  <div class="row">
    <% if @programs.present? %>
      <%= render @programs %>
    <% else %>
      <%= link_to 'Add your first client', new_program_path %>
    <% end %>
  </div>
</div>
