<!DOCTYPE html>
<html>
<head>
  <title>Right Side Health</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <script type="text/javascript" src="//use.typekit.net/wfv3edg.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
  <%= yield :script %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header col-lg-8 col-md-7 col-sm-6">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to image_tag('logo.png'), root_path, class: 'logo' %>
        <% if current_coach %>
        <div class="welcome">
          <div class="top">
            <h4>Welcome, Adam</h4>
          </div>
          <div class="bottom">
            <p>
              <% if current_coach %>
                <%= link_to 'Sign out', destroy_coach_session_path, method: :delete %>
              <% else %>
                <%= link_to 'Sign in for coaches', new_coach_session_path %></li>
              <% end %>
            </p>
          </div>
        </div>
        <% end %>
        <ul class="nav navbar-nav">
          <% if can? :manage,  current_practice %>
            <li class="<%= 'active' if params[:controller] == 'practice' %>"><%= link_to 'Admin', practice_path(current_practice) %></li>
          <% end %>
        </ul>
      </div>
      <div class="navbar-collapse collapse">
      </div>
      <div class="navbar-header col-lg-4 col-md-5 col-sm-6">
        <form class="navbar-form navbar-right" role="search">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="find a patient">
          </div>
          <button type="submit" class="btn btn-primary glyphicon glyphicon-search"></button>
        </form>
      </div>
    </div>
  </nav>

  <%= render '/common/notification' %>

  <%= yield %>

</body>
</html>
