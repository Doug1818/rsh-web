<div class="payment-errors alert alert-error"></div>

<%= simple_form_for @practice, html: { class: "form-vertical" } do |f| %>
  <lengend>Your practice</lengend>
  <%= f.input :name %>

  <lengend>Your account</lengend>
  <%= f.fields_for :coaches do |f| %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :password_confirmation %>
  <% end %>

  <legend>Billing Info</legend>
  <div class="form-group string required">
    <label class="string optional control-label">Card Number</label>
    <input class="card-number string optional form-control" maxlength="255" size="255" autocomplete="off" type="text">
  </div>
  <div class="form-group string required">
    <label class="string optional control-label">CVC</label>
    <input class="card-cvc string optional form-control" maxlength="255" size="255" autocomplete="off" type="text">
  </div>
  <div class="form-group string required">
    <label class="string optional control-label">Expiration (MM/YYYY)</label>
    <br/>
    <input class="card-expiry-month string optional form-control" maxlength="2" size="2" autocomplete="off" type="text">
    <span> / </span>
    <input class="card-expiry-year string optional form-control" maxlength="4" size="4" autocomplete="off" type="text">
  </div>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>
