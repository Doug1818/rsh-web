<%= simple_form_for @supporter, html: { class: "form-vertical" } do |f| %>
  <%= f.hidden_field :program_id %>
  <%= f.input :email, label: false, placeholder: 'Email' %>

  <%= f.submit "Add Supporter", class: 'btn btn-primary' %>
<% end %>
