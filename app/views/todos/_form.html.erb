<%= simple_form_for @todo, html: { class: "form-vertical" } do |f| %>
  <%= f.hidden_field :program_id %>
  <%= f.input :body, label: false, placeholder: 'To-Do message' %>

  <%= f.submit "Add To-Do", class: 'btn btn-primary' %>
<% end %>
